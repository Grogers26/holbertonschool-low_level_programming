#include <stdio.h>
#include "main.h"
/**
 * mul - multipling two integers
 * @a: the first number
 * @b: the second number 
 * Return: the product of a and b
 */
int mul(int a, int b)
{
return (a * b);
}
