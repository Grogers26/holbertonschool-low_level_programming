#include "main.h"
#include<stdio.h>
/**
 *_isupper - checking for uppercase letter. using isupper and return.
 * @c: checker
 * Return: no return
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
