#include <stdio.h>
#include "main.h"
/**
*main - print numbers 0 - 9 while only using _putchar twice in the code
*
*Return: always (void)
*/
void  print_number(void)
{
  for(int i = 0; i < 10; i++)
    {
      _putchar("%\n" + i);
    }
  {
    int main(void)
    {
      for (int i = 0; i < 10; i++)
	{
	_putchar("%d\n", i);
      }
      return 0;
    }
  }
}
